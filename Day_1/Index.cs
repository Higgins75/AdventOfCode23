public class Index
{
    public bool checkFirst(int initialIndex, int CurrentIndex)
    {
        return CurrentIndex <= initialIndex;
    }

    public bool checkLast(int initialIndex, int CurrentIndex)
    {
        return CurrentIndex >= initialIndex;
    }
}